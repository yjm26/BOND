{
  "name": "bond-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test test/BOND.test.js",
    "deploy": "hardhat run scripts/deploy-phase-a.js --network arcTestnet",
    "verify": "node scripts/verify-arcscan.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  },
  "description": "BOND Arc Testnet escrow contracts."
}
