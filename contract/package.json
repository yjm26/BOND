{
  "name": "bond-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "npm run deploy:bound-testnet",
    "deploy:bound-testnet": "hardhat run scripts/deploy-bound-testnet.js --network arcTestnet",
    "test:bound-testnet": "hardhat test test/BOND.test.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  },
  "description": "BOND Arc Testnet escrow contracts."
}
